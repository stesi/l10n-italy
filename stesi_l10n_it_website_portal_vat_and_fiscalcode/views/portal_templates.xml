<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template id="address_b2b_fiscalcode" inherit_id="website_sale.address" name="Show b2b fields fiscalcode" customize_show="True">
        <xpath expr="//div[@id='div_phone']" position="after">
            <t t-if="website_sale_order.partner_id.country_id == request.env.ref('base.it') and mode == ('new', 'billing') or (mode == ('edit', 'billing') and (can_edit_vat or 'vat' in checkout and checkout['vat']))">
                <div t-attf-class="form-group #{error.get('fiscalcode') and 'o_has_error' or ''} col-lg-6 div_fiscalcode">
                    <label class="col-form-label font-weight-normal label-optional" for="vat">Fiscal Code </label>
                    <t t-set="vat_not_editable_message">Changing Fiscal Code number is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.</t>
                    <input type="text" name="fiscalcode" t-attf-class="form-control #{error.get('fiscalcode') and 'is-invalid' or ''}" t-att-value="'fiscalcode' in checkout and checkout['fiscalcode']" t-att-readonly="'1' if 'fiscalcode' in checkout and checkout['fiscalcode'] or not can_edit_vat else None" t-att-title="vat_not_editable_message if 'fiscalcode' in checkout and checkout['fiscalcode'] and not can_edit_vat else None" />
                </div>
            </t>
        </xpath>
        <xpath expr="//input[@name='vat']" position="attributes">
            <attribute name="t-att-readonly">'1' if 'vat' in checkout and checkout['vat'] or not can_edit_vat else None</attribute>
        </xpath>
    </template>

    <template
        id="portal_my_details"
        name="Add Fiscalcode and Vat in Portal"
        inherit_id="portal.portal_my_details">

        <xpath expr="//div[hasclass('o_portal_details')]/div[1]/div[1]/div[7]" position="after">
            <div t-attf-class="form-group #{error.get('fiscalcode') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label label-optional" for="fiscalcode">Fiscal Code</label>
                <input
                    type="text"
                    name="fiscalcode"
                    t-attf-class="form-control #{error.get('fiscalcode') and 'is-invalid' or ''}"
                    t-att-value="fiscalcode or partner.fiscalcode"
                    t-att-readonly="None if partner.can_edit_vat() and not partner.vat else '1'"
                    t-att-title="None if partner.can_edit_vat() else 'Changing Fiscal Code is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.'"/>
            </div>
            <t t-if="partner.country_id == request.env.ref('base.it')">
                <div t-attf-class="form-group #{error.get('vat') and 'o_has_error' or ''} col-xl-6">
                    <label class="col-form-label label-optional" for="vat">Vat</label>
                    <input
                        type="text"
                        name="vat"
                        t-attf-class="form-control #{error.get('vat') and 'is-invalid' or ''}"
                        t-att-value="vat or partner.vat"
                        t-att-readonly="None if partner.can_edit_vat() and not partner.vat else '1'"
                        t-att-title="None if partner.can_edit_vat() else 'Changing Fiscal Code is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.'"/>
                </div>
            </t>
        </xpath>
    </template>
</odoo>
